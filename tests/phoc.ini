[core]
xwayland=false

[output:HEADLESS-1]
mode=1024x768

# Same size when running nested against a wayland session, which is how the
# GPU dependent paths get exercised (the x11 setup only ever gets pixman)
[output:WL-1]
mode=1024x768
